<h1>Listing Upc's</h1>

<table>
  <tr>
    <th>Upc</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @scripts.each do |script| %>
  <tr>
    <td><%= link_to script.upc, script %></td>
    <td><%= link_to 'Edit', edit_script_path(script) %></td>
    <td><%= link_to 'Destroy', script, method: :delete, data: { confirm: 'Are you sure?' }, id: "red" %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New UPC', new_script_path, id: "blue" %>